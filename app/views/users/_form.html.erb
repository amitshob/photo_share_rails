<%= render('layouts/errors', :object => @user) %>

<div class= "container">
  <%= form_for [@photo, @user] do |f| %>
    <%= f.label :username %><br>
    <%= f.text_field :username %>
    <%= f.label :name %><br>
    <%= f.text_field :name %>

    <p><%= f.submit %></p>
  <% end %>
<div>
